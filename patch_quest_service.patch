--- godot/core/services/quest_service.gd
+++ godot/core/services/quest_service.gd
@@ -23,9 +23,10 @@
				"tasks": []
			}

-		var task = str(row.get("task", ""))
-		if task != "":
-			quests_dict[quest_id]["tasks"].append(task)
+		var raw_task = row.get("task")
+		if raw_task != null:
+			var task = str(raw_task)
+			if task != "":
+				quests_dict[quest_id]["tasks"].append(task)

	var result: Array[Dictionary] = []
	for key in quests_dict:
